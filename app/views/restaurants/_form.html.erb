<%= simple_form_for(restaurant) do |f| %>
<%= f.error_notification %>
<%= f.error_notification message: f.object.errors[base].t%>

<div class= "form-inputs">
  <%= f.input :name %>
  <%= f. input :address %>
  <%= f. input :rating %>
  </div>

  <div class= "form-actions">
    <%= f.button :submit %>
    </div>
<% end %>
